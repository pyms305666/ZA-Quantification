# 练习02：读 main.py 回答问题（理解题）

> 对应 docs/10 第 3 章。打开项目根目录的 `main.py` 对照着读，把答案写在每个问题下面的"答："后面。
> 写完对照 `答案/练习02-参考答案.md`（答案只给要点，你用自己的话也行）。

## 问题 1：这个文件运行后，程序会"停"在哪一行？为什么不会执行到文件末尾就结束？

（提示：找到 `uvicorn.run(...)` 那一行。`uvicorn.run` 是"开始提供网站服务"，它执行后函数不会返回——
就像餐厅开门营业后，"营业"这个动作一直持续到你打烊。）

答：

## 问题 2：`python main.py --debug-stack` 和 `python main.py` 有什么区别？哪一行代码造成的？

（提示：`sys.argv` 是命令行参数列表，`--debug-stack` 出现在列表里就会开启一个"每 30 秒打印所有线程调用栈"的诊断器。）

答：

## 问题 3：`if __name__ == "__main__":` 这行删掉会怎样？会有什么副作用？

（提示：想象另一个文件写了 `import main` 来借用里面的函数——删除后 import 的瞬间会发生什么？）

答：

## 问题 4：`faulthandler.dump_traceback_later(30, repeat=True)` 是干什么用的？什么场景下你会感谢它？

（提示：程序"卡死"的时候，你可能想知道每个线程都停在哪一行。）

答：

## 问题 5（动手）：把 `main.py` 复制一份改名 `main_test.py`，把端口改成 8001 运行，
然后浏览器同时打开 8000 和 8001 验证"两个服务可以并存"。做完了在这里打 ✅

答：

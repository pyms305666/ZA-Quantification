# 项目检查报告（2026-09-11）

## 1. 检查目标

本报告针对 `E:\codx project\期货评估器` 当前工作区进行盘点，目标是：

- 让 README 与实际代码、启动方式和产物保持一致；
- 记录当前能确认的问题，不把未验证的功能写成“已完成”；
- 保留用户现有未提交改动，不删除、不回滚已有文件。

本轮只修改文档：根目录 `README.md`、`electron/README.md`，并新增本报告。

## 2. 检查范围

已查看：

- Python 启动器、配置、REST/WebSocket API；
- `market/` 行情缓存、指标和评估代码；
- `tq/` 兼容层与 `tqdiff/` DIFF 认证/行情客户端；
- Electron 主进程和说明文档；
- `mobile-app/` Android/Chaquopy 工程及 Python 副本；
- `tests/`、`docs/`、打包配置和 `dist/` 产物；
- 当前 Git 工作区状态。

## 3. 可复现验证

### 3.1 Python 单元测试

命令：

```powershell
python -m unittest discover -s tests -v
```

结果：**43 个测试通过（OK）**。

这说明当前测试覆盖的单元逻辑通过，不代表真实行情服务器、网络认证、历史 K 线、Electron 或 Android 真机已经通过验收。

### 3.2 Python 语法/字节码检查

命令：

```powershell
python -m compileall .
```

结果：**通过**。

### 3.3 Android 构建条件

检查结果：

- `mobile-app/gradle/wrapper/` 只有 `gradle-wrapper.properties`；
- `mobile-app/gradlew` 不存在；
- `mobile-app/gradlew.bat` 不存在；
- 系统 PATH 未发现 `gradle`；
- 本机虽有 Java 17，但缺少可直接调用的 Gradle wrapper/命令。

结论：仓库中已有 APK 文件，但本轮无法重新构建，因此不能声称 Android 源码构建可复现。

## 4. 已确认问题

| 编号 | 严重度 | 问题 | 影响 | 建议 |
| --- | --- | --- | --- | --- |
| P1 | 高 | 历史 K 线真实链路未闭环 | 曾出现 `HISTORY_EMPTY`，可能影响指标和决策 | 在真实账号、交易时段逐合约验证 `set_chart` 响应，保存原始响应和字段映射；没有数据时显式返回“历史不可用” |
| P1 | 高 | Android 构建不可复现 | 无法从当前工作区确认移动端改动是否能重新打包 | 补齐 wrapper 脚本和校验文件，或明确要求安装匹配 Gradle/SDK，并在 CI/干净环境构建 |
| P1 | 高 | Electron 依赖系统 PATH 的 `python` | 某些 Windows 环境中 `python` 不存在或指向错误版本 | 启动时查找 `py -3`、虚拟环境 Python 和打包后的后端；把实际命令写入日志 |
| P1 | 高 | Electron 后端子进程日志被忽略 | 启动失败时只有“超时”，定位困难 | 将 stdout/stderr 写入日志文件或转发到主进程日志；处理 child `error`/`exit` |
| P2 | 中 | `electron/main.js` 使用未声明的 `portBlockedBy` | 依赖非严格模式下的隐式全局变量，维护风险高 | 在模块级声明 `let portBlockedBy = null`，每次启动前重置 |
| P2 | 中 | 桌面端和移动端有大量重复 Python 代码 | 修复容易只改一边，行为逐渐分叉 | 抽取共享核心包，或建立自动同步/差异检查；至少在报告和发布流程中明确版本对应关系 |
| P2 | 中 | 桌面与移动端 `tqdiff/auth.py` 已出现明显差异 | 缓存格式、gzip/流式索引和 TTL 行为不同 | 统一协议实现和缓存契约，分别用桌面/Android 测试覆盖 |
| P2 | 中 | 凭据明文保存到 `.tqsdk/credentials.json` | 本机文件泄露会暴露账号密码 | 优先使用环境变量；后续改用 Windows Credential Manager/Android Keystore 等系统密钥存储 |
| P2 | 低 | `api/http.py`、`tqdiff/client.py` 有重复导入和清理项 | 不影响当前主要功能，但降低可读性 | 删除重复/未使用导入并增加 lint 检查 |
| P2 | 低 | README、Electron README 曾互相矛盾 | 用户可能误以为必须手动启动后端，或误以为 Electron 是完整绿色版 | 本轮已同步两份 README，并明确自动拉起和打包边界 |
| P2 | 低 | 200ms 端到端更新目标没有基准证明 | 不能保证界面显示延迟达到目标 | 增加带时间戳的服务端接收、WebSocket 发送、浏览器渲染延迟统计 |

## 5. 已有成功记录与未验证边界

根据此前调试记录：

- 实时行情曾成功返回 `rb2610` 的最新价、成交量、持仓量和买卖价；
- 历史 K 线曾返回响应但列/行为空：

```json
{
  "error": "HISTORY_EMPTY",
  "detail": "responses=1/2, fields=10, bad=True, size=30, cols=0, rows=0"
}
```

因此当前应诚实表述为：实时行情链路有成功样例，但历史 K 线、端到端 200ms 更新、Electron 打包后自动拉起和 Android 真机行情尚未完成本轮真实验收。

## 6. 当前工作区改动说明

用户已有的代码改动和未跟踪文件均保留，未做删除或回滚。本轮新增/修改：

- `README.md`：按实际 C 直连架构、启动方式、验证状态和已知限制重写；
- `electron/README.md`：修正 Electron 自动启动和打包边界；
- `docs/项目检查报告-2026-09-11.md`：本报告。

## 7. 推荐修复顺序

1. 先补历史 K 线的真实协议验证和字段测试；
2. 修复 Electron 的 Python 查找、日志、子进程异常和隐式全局变量；
3. 统一桌面/移动端 DIFF 认证与合约缓存实现；
4. 增加 200ms 更新的时间戳指标和压力测试；
5. 补齐 Android wrapper/CI，完成干净环境构建；
6. 将明文凭据迁移到系统安全存储。

## 8. 诚实结论

项目的 Python 单元测试和编译检查是通过的，代码结构也已经具备 REST、WebSocket、桌面壳和移动端工程。但“能稳定获取真实行情、历史 K 线完整、200ms 端到端更新、Electron/Android 可重复构建”目前不能全部宣称完成。最关键的未闭环项是历史 K 线真实数据链路、Electron 启动可诊断性和 Android 构建可复现性。

# ZA量化 · 项目交接文件

> 本文件面向接手本项目的开发者/协作者，帮助在最短时间内理解项目全貌、当前状态、
> 正在进行的修复、以及已知的技术债与坑。
>
> **生成日期**：2026-09-06
> **当前分支**：`mobile-app`（手机版开发分支；当前未提交改动集中在手机版）
> **主分支**：`master`
> **版本**：V1.1.1（桌面 C 直连版 / 手机版均已到 v1.1.1）

---

## 0. 一分钟看懂这个项目

**ZA量化** = 一个「国内期货行情网关 + 决策评估系统」，提供**行情读取、合约评估（多/空/观望 + 止损/目标/风险）、专业 K 线界面**。**不涉及下单与自动交易。**

```
┌──────────┐   DIFF 协议   ┌────────────┐   订阅/推送   ┌───────────────┐
│  天勤行情  │ ◄─────────── │  后端数据层  │ ───────────► │  前端 Web UI   │
│  服务器    │              │ tqdiff/    │              │ 行情/K线/决策   │
└──────────┘              └────────────┘              └───────────────┘
```

- **桌面版**：后端跑在本机 `127.0.0.1:8000`（FastAPI + uvicorn），Electron 或浏览器打开前端。
- **手机版**：**Chaquopy 内嵌 Python**，后端也跑在手机本地 `127.0.0.1:8000`，WebView 加载同源前端（**无云服务器**）。这是与 `docs/15-手机版方案.md` 里"云服务器 + PWA"设想**不同的最终落地**（见 §5）。

---

## 1. Git 分支结构

| 分支 | 路线 | 说明 |
|---|---|---|
| `master` | — | 主分支，稳定版 |
| `route-ab-sync-coroutine` | **A+B 协程路线** | 保留 TqSdk 的渐进修复（TqApi 单事件循环 + 命令队列 + Future 修复） |
| `route-c-diff-direct` | **C 直连路线** | 弃用 TqSdk，直连天勤 DIFF 协议（`tqdiff/`），K线/订阅协程并发，无队列活锁 |
| `mobile-app` | 手机版 | **当前所在分支**，基于 C 路线的 Chaquopy 内嵌 APK |

> `route-ab-sync-coroutine` 与 `route-c-diff-direct` 是上一阶段"开两条平行技术路线验证 K 线阻塞"的产物，
> 都有各自的打包 exe（`dist/ZA量化-AB协程版.exe` / `dist/ZA量化-C直连版.exe`）。
> **当前重心在手机版（`mobile-app` 分支），桌面双路线视为已稳定。**

**注意**：根目录同时存在 `tq/`（适配层）与 `tqdiff/`（DIFF 核心），且同一份代码在 `mobile-app/app/src/main/python/` 下有一份**拷贝**。改桌面版 vs 手机版的入口不同（见 §4、§6）。

---

## 2. 核心目录结构（根目录）

```
期货评估器/
├── main.py                  # 桌面版启动入口（FastAPI + 静态前端）
├── launcher.py              # 桌面版打包/启动器（自动生成 config、检测端口占用）
├── config.py                # 配置加载：config.json + 本地凭据 + 环境变量（凭据在 .tqsdk/credentials.json）
├── services.py              # 运行时服务装配（桌面版与手机版共用 build_services）
├── mobile_api.py            # 手机版接口层（starlette，非 fastapi；与桌面 REST/WS 协议一致）
├── api/                     # 桌面版 HTTP/WS 路由（FastAPI）
│   ├── http.py              #   含 /api/v1/auth、/status、/instruments、/quote、/kline、/decision
│   └── websocket.py         #   WS 连接管理（ConnectionManager）
├── market/                  # 决策评估核心（纯逻辑，无 IO）
│   ├── cache.py             #   带锁行情缓存 QuoteCache
│   ├── evaluator.py         #   决策评估引擎（多空分数/方向/止损/目标/风险）
│   ├── indicators.py        #   技术指标（MA/MACD/ATR 等）
│   ├── model.py             #   数据模型（MarketQuote/Instrument/…）
│   └── processor.py         #   行情实体 -> MarketQuote 转换
├── tq/                      # 与 TqClient 的兼容适配层 + 合约发现/订阅
│   ├── client.py            #   仅做兼容 re-export（C 路线下指向 tqdiff.client）
│   ├── instruments.py       #   合约发现 InstrumentManager
│   └── subscriber.py        #   动态订阅 SubscriptionManager
├── tqdiff/                  # 【C 路线核心】DIFF 协议直连
│   ├── auth.py              #   OAuth 登录 / 名称服务 / 合约文件下载（gzip + ijson 流式）
│   ├── client.py            #   DiffClient：主循环 / 订阅 / K线 / 快照 / 目录加载
│   └── symbol_index.py      #   【新增】合约文件精简索引（ijson 流式解析 + pickle 持久化）
├── static/                  # 桌面版前端（HTML/CSS/JS，ECharts）
├── docs/                    # 教学与方案文档（见 §7）
├── design/                  # 手机版 UI 设计稿 / 高保真图
├── exercises/               # 软件工程教学练习
├── electron/                # Electron 桌面壳（菜单/窗口/自动起后端）
├── installer/               # Inno Setup 安装器脚本（ab.iss / c.iss）
├── mobile-app/              # 【手机版】Android 工程（Chaquopy + WebView）
└── dist/                    # 打包产物（exe / setup exe / zip / apk）
```

---

## 3. 数据流（核心心智）

### 行情链路
1. 前端 WebSocket 连 `/ws/market`，发 `{action:"subscribe", symbols:[...]}`。
2. 后端 `SubscriptionManager.subscribe` → `DiffClient._subscribe` → 向天勤发 `subscribe_quote`。
3. 天勤推 `diff` → `DiffClient._apply_diff` 合并 → `_dispatch_quote` → `on_quote_change` 回调。
4. 回调写缓存 `QuoteCache` + 投递异步广播队列 → 前端收到 `quote` / `quote_snapshot` 实时渲染。

### K 线链路
- 前端请求 `/api/v1/kline/{symbol}?period=&count=` → 后端 `_get_kline` 向天勤 `set_chart`，缓冲异步到达后返回。

### 决策评估链路
- 前端请求 `/api/v1/decision/{symbol}` → 后端拉多周期 K 线 + 最新行情 → `market.evaluator` 计算多空分数/方向/止损/目标/风险。

### 合约目录链路（当前正在修的重点）
- 天勤的**合约文件** `latest.json`（线上约 334MB，约 23.9 万条合约元数据）**只服务于"搜索合约/名称/乘数"**。
- **订阅行情 / K线 / 盘口 / 决策评估都不依赖它**——行情服务器只认合约代码。
- 目录加载：`DiffClient._load_symbol_file` 后台执行，缓存优先，无缓存则下载（见 §5）。

---

## 4. 桌面版与手机版的差异（重要）

桌面版与手机版**复用同一套后端逻辑**（`market/`、`tqdiff/`、`services.py`、`mobile_api.py`），
区别只在**入口与环境**：

| 维度 | 桌面版（C 路线） | 手机版 |
|---|---|---|
| 入口 | `api/http.py`（FastAPI） | `mobile_api.py`（starlette，avoid pydantic 原生依赖） |
| 启动 | `main.py` | `backend_main.py` 由 Chaquopy 调用 `run_mobile_server` |
| 端口 | `127.0.0.1:8000` | `127.0.0.1:8000`（App 内） |
| 前端 | `static/` | `mobile-app/app/src/main/python/static/`（独立一份） |
| 数据源 | `tqdiff/`（DIFF） | 同样 `tqdiff/`（DIFF）——但手机版有一份**拷贝** |

> ⚠️ **避坑**：改手机版逻辑要去 `mobile-app/app/src/main/python/` 下改，不要改根目录那份（那是桌面的）。
> 手机版的 `tqdiff/`、`tq/`、`market/`、`services.py`、`config.py`、`mobile_api.py`、`static/`
> **全部在 `mobile-app/app/src/main/python/` 下有拷贝**，需同步修改。

---

## 5. 手机版架构（与文档的差异，务必先读）

### 5.1 最终落地（实际代码）
- **Chaquopy 内嵌 Python** + **WebView**，后端在**手机本地** `127.0.0.1:8000` 运行。
- **无云服务器**。这与 `docs/15-手机版方案.md` 里"手机 App 连云服务器后端（PWA/Capacitor）"的设想**不同**。
- 之所以改：用户要求"独立、不连电脑/服务器"，Chaquopy 内嵌正好满足。

### 5.2 手机版模块划分（`mobile-app/app/src/main/`）
```
python/                          # Chaquopy 打包的 Python 后端
├── backend_main.py              #   Android 入口：模块导入即启动一次后端（线程内）
├── config.py                    #   配置 + 凭据（写 App 私有目录 za.filesdir）
├── mobile_api.py                #   starlette 接口层 + run_mobile_server
├── services.py                  #   build_services（复用）
├── market/                      #   决策评估（复用）
├── tq/                          #   适配层（复用）
├── tqdiff/                      #   DIFF 核心（复用；含新增 symbol_index.py）
│   ├── auth.py                  #     登录/名称服务/合约文件下载（gzip+ijson）
│   ├── client.py                #     DiffClient
│   └── symbol_index.py          #     【新增】合约精简索引
└── static/                      #   手机版前端（6 屏：行情/K线/盘口/决策/我的/登录）
java/com/zaquant/mobile/MainActivity.java   # WebView + Python.start + waitAndLoad
build.gradle                     #   AGP 8.2.2 + Chaquopy 16.1 + pip 依赖
```

### 5.3 手机版关键机制
- `MainActivity.java`：`Python.start(new AndroidPlatform(this))`（Chaquopy 16.x 新 API），
  启动前 `System.setProperty("za.filesdir", getFilesDir().getAbsolutePath())`。
- `backend_main.files_dir()`：读 `za.filesdir` 或 `TQ_MOBILE_FILES_DIR`，判定 App 私有可写目录。
- WebView 轮询 `http://127.0.0.1:8000/api/v1/status` 直到 200 再 `loadUrl`。

---

## 6. 合约目录下载 —— 当前正在修的问题（重点）

### 6.1 现象（用户真机反馈）
1. 打开 App 显示"正在下载合约目录"，**等了 5~6 分钟甚至更久**仍不显示。
2. **下载到 4MB 左右又归零**，从头重下。
3. 自选价格 `--`、搜索不可用（虽已标记"行情已可用"）。

### 6.2 根因链（已查明）
| 根因 | 说明 |
|---|---|
| **服务器限速** | 天勤对 `latest.json` 下载限速在 **约 0.5 MB/s**。实测电脑端下载同一文件 120 秒也只到 15MB，`catalog_ready=False`。**与手机网络无关，对谁都慢。** |
| **文件巨大** | 明文 JSON **334MB**（`Content-Length: 350174892`）。 |
| **旧代码 json.loads OOM** | 之前 `download_symbol_file` 下载后 `json.loads(tmp.read_text())` 把 334MB 一次读入 → 手机内存爆 → 进程被杀 → **"下载到一半又从 0 重下"**。 |
| **gzip 未利用** | 服务器支持 gzip，压缩后仅 **8.3MB（30 倍压缩）**，但旧代码没显式请求。 |
| **`raw.stream(chunk_size=)` 参数错误** | urllib3 的 `stream()` 参数是 **`amt=`** 不是 `chunk_size`；用错会抛 `TypeError` → 被当"下载中"卡住（已修复为 `amt=`）。 |
| **断点续传缺失** | 下载中途失败（弱网/超时）会 `tmp.unlink()` 删掉临时文件，下次**从 0 重新下载**——这就是"4MB 归零"。 |

### 6.3 已做的修复（`mobile-app` 分支未提交改动）
- **gzip 下载**：`auth.py` 请求加 `Accept-Encoding: gzip`，`response.raw.stream(amt=256*1024, decode_content=False)` 拿压缩字节直接落盘 `.gz`，只写 ~8.3MB。
- **ijson 流式解析**：新增 `symbol_index.py`，用 `ijson.kvitems` 边解压边逐条解析成**精简 record**（不一次性加载全量）。
- **pickle 索引持久化**：解析结果存 `symbol_index.pkl`（约 25.6MB，238877 条），**二次打开 0.28 秒秒开**。
- **增量就绪**：`_on_index_progress` 解析到 1000 条即 `_file_loaded.set()`，搜索尽早可用。
- **前端门控**：目录未就绪时 `loadInstruments` 不再反复请求/报错，只提示"目录下载中"。
- **前端自选价格缓存**：`state.quotes` 缓存 + `renderWatchlist` 重建后回填价格（解决切屏价格变 `--`）。
- **订阅不等待目录**：`_subscribe` 改用 `_file_loaded.is_set()` 直接判断，不 `_wait_file`，行情即时可用。

### 6.4 仍未解决（接下来的坑）
- **根因仍是"天勤服务器下载限速 0.5MB/s"**，即便 gzip 后仅 8.3MB，网络层也要 ~17 秒（弱网更久）。
- **没有断点续传**：下载失败仍会把 `.tmp` 删除，从 0 重下。**这是"4MB 归零"的遗留根因**。
- **`load_cached_symbol_file` 的 TTL**：手机版已改成 `SYMBOL_CACHE_TTL = 7*24*3600`（原本桌面版是 24h）。
- **桌面版 `tqdiff/auth.py` 的 TTL 仍是 24h**，且桌面版 `download_symbol_file` **未**同步 gzip/ijson 改造（桌面那份还是旧 256MB 全量 + `json.loads`）。

---

## 7. 文档体系（docs/）

| 文件 | 内容 |
|---|---|
| `01-架构总览.md` | 整体架构 |
| `02-Electron入门.md` | Electron 桌面壳 |
| `03-接口调用教学.md` | 前后端接口约定 |
| `04-前端三件套.md` | HTML/CSS/JS |
| `05-实战：从零写行情卡片.md` | 前端实战 |
| `06-现有代码导读.md` | static/app.js 逐段导读 |
| `07-问题分析与修复方案-首屏K线阻塞.md` | K 线阻塞根因与修复 |
| `15-手机版方案.md` | 手机版方案（**注意：描述的是"云服务器+PWA"，与实际 Chaquopy 内嵌不同**） |
| `手机版问题-02-解决方案.md` | 手机版第二次修复（目录下载/自选价格）方案（**已审批实施**） |
| `软件工程学习/` | 教学材料 |

> ⚠️ **文档与代码不同步**：`docs/15` 描述云服务器架构，实际是 Chaquopy 内嵌。建议接手后择机更新此文档。

---

## 8. 当前未提交改动（git status）

当前分支 `mobile-app`，有这些未提交改动（都是手机版修复，**尚未 commit**）：

- `mobile-app/app/build.gradle`（加 ijson==3.2.3 依赖）
- `mobile-app/app/src/main/java/com/zaquant/mobile/MainActivity.java`（设 za.filesdir）
- `mobile-app/app/src/main/python/backend_main.py`（files_dir + TQ_MOBILE_FILES_DIR）
- `mobile-app/app/src/main/python/config.py`（clear_credentials + 移动目录）
- `mobile-app/app/src/main/python/mobile_api.py`（auth 三态 + status 加 catalog 进度）
- `mobile-app/app/src/main/python/static/app.js`、`index.html`、`style.css`（登录/自选/门控/缓存）
- `mobile-app/app/src/main/python/tq/instruments.py`（限流）、`tq/subscriber.py`（降级订阅）
- `mobile-app/app/src/main/python/tqdiff/auth.py`（gzip + ijson 流式）、`tqdiff/client.py`（解耦目录）
- **新增** `mobile-app/app/src/main/python/tqdiff/symbol_index.py`（未跟踪）

> ⚠️ `local.properties`（含 SDK 路径）在 `.gitignore` 内，不会被提交；但构建需要它。

---

## 9. 如何构建 / 运行

### 9.1 桌面版（Windows）
```bash
# 直接跑（开发）
python main.py                     # 或 .venv 里的 python

# 打包单文件 exe（C 路线）
pyinstaller --noconfirm --clean --onefile --name "ZA量化-C直连版" \
  --icon assets/icon.ico --add-data "static;static" \
  --add-data "config.json.example;." --add-data "VERSION;." launcher.py

# 安装器（Inno Setup）
#   installer/c.iss  -> C 路线；installer/ab.iss -> A+B 路线
```

### 9.2 手机版 APK（Android，需 JDK17 + AGP 8.2.2 + Android SDK 34）
```bash
cd mobile-app
# local.properties 需含 sdk.dir=E:/android-sdk（已在 gitignore）
JAVA_HOME="..."  E:/tools/gradle-8.2/bin/gradle.bat assembleDebug
# 产物：mobile-app/app/build/outputs/apk/debug/app-debug.apk
# 复制到 dist：cp .../app-debug.apk "dist/ZA量化-手机版-v1.1.1.apk"
```
> 构建用 `E:/tools/gradle-8.2`（系统里没全局 gradle，wrapper dists 是空的）。Chaquopy 16.1。

### 9.3 手机版关键配置（build.gradle 的 python 块）
```gradle
python {
    pip { install "uvicorn==0.30.6"; install "starlette==0.38.6"; ...; install "ijson==3.2.3" }
}
```
> 注意：`python {}` 块必须放在 `android.defaultConfig` **内部**（Chaquopy 的 createDsl 挂在 defaultConfig 上），
> 放 `android{}` 里会报 "Could not find method python()"。且需 `ndk { abiFilters "x86_64","arm64-v8a" }`。

---

## 10. 已知问题 / 技术债清单

1. **合约目录下载慢 + 4MB 归零**：服务器限速 + 无断点续传。**这是当前最高优先级的未解决问题。**
   建议优先加**断点续传**（保留 `.tmp` 别删，重启续传）或改**内置精简合约表**绕开大文件下载。
2. **桌面版与手机版代码双份**：改一处需同步另一处，易漏。
3. **文档不同步**：`docs/15` 描述的云服务器架构 vs 实际 Chaquopy 内嵌。
4. **桌面版 `tqdiff/auth.py` 未同步 gzip/ijson 改造**：桌面仍是旧 256MB + `json.loads`，TTL 还是 24h。
5. **`local.properties`**（SDK 路径）在 gitignore 里，换机器构建需重建。
6. **`config.json`（含真实天勤账号）**：**已在 `.gitignore`（第 2 行）且未被 git 跟踪**——账号密码不会进仓库（安全）。
   `credentials.json` 在 `.tqsdk/`（第 11 行亦被 gitignore）。接手时保持这两条 ignore 规则即可。

---

## 11. 下一步建议（按优先级）

1. **解决"合约目录下载 4MB 归零"**（用户当前最痛）：
   - 最稳妥：**断点续传**（下载失败不删 `.tmp`，且记录已下载字节，下次 `Range` 续传）。
   - 或更彻底：**内置精简合约表**（主流交易所合约，几 KB），彻底绕开 334MB/8.3MB 下载。
2. **确认 `config.json` 不含敏感信息且被 gitignore**（安全）。
3. **同步桌面版 `tqdiff/auth.py`**（gzip/ijson/TTL 与手机版对齐），或明确桌面版不再演进。
4. **更新 `docs/15`** 反映 Chaquopy 内嵌实际架构。
5. **提交当前 `mobile-app` 分支未提交改动**（经测试后），必要时发 GitHub Release 更新 APK。

---

## 12. 快速自查清单（接手测试）

- [ ] 桌面版能连天勤、行情显示、K 线正常（电脑端）
- [ ] 手机版 APK 安装后能登录、行情/自选价格显示
- [ ] 合约目录：首次下载能推进（不再 4MB 归零）、二次打开秒开（读索引）
- [ ] 目录下载期间行情/K线/决策不受影响（只影响搜索）
- [ ] 切换界面后自选价格不丢失（`state.quotes` 缓存回填）
- [ ] `config.json` / `credentials.json` 不含明文密码且不进仓库

---

*本交接文件基于 2026-09-06 的仓库状态编写，后续若有代码演进请同步更新。*
